# Provenance: 02 — Cursor Parser

## Session

- Session: Initial child-plan write (Jun 14, 2026)
- Timestamp: 2026-06-14T00:00:00Z
- CWD: /home/codyh/workspace/workflow-plugin

## Initial Request (verbatim)

> Start with opencode and cursor. We'd want to index conversation logs, store them efficiently, then provide a queryable interface.

## Planning Conversation

### Turn 1

**Agent asked:** Should this child plan add a real Cursor fixture (built by running Cursor) or a synthetic one?

**Agent decisions:**

- Synthetic. The same rationale as child 01: real Cursor sessions may contain private content. The fixture is generated by `tests/fixtures/cursor/build_fixtures.py`.
- The implementation must inspect a real Cursor session's directory layout to confirm the file naming and JSONL structure. That inspection happens during execution; the plan assumes the structure documented in `skills/transcript-parser/SKILL.md` (which was written from prior observation).

### Turn 2

**Agent asked:** Does Cursor store subagent transcripts separately?

**Agent decisions:**

- Per the existing `transcript-parser` skill: no. Cursor's `agent-transcripts/{session}/` directory contains a single conversation, not a parent-with-subagents hierarchy. The cursor parser emits a single parent agent.
- If a future Cursor version adds subagents, the parser is the place to extend. The schema and library types already support subagents; the parser just doesn't discover them.

## Agent Decisions

- **No schema changes.** The cursor parser fits the existing schema. This child plan's primary value is validating the abstraction generalizes; it is not an opportunity to refine the schema.
- **Cross-tool aggregation test is required.** A test that queries across both opencode and cursor data confirms the canonical tool-name enum is doing its job. This test is the cheap insurance against cursor-specific naming regressions.
- **Implementation must inspect a real Cursor session.** The plan assumes the directory layout from the existing skill, but a real install is the ground truth. If the layout has changed, the parser is updated; if a redesign is needed, that's a DESIGN.md issue surfaced before child 03.
